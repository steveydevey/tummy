<!DOCTYPE html>
<html style="background-color: #ffffff; color: #000;">
  <head>
    <style>
      body, h1, h2, h3, h4, h5, h6, p, a, span, div {
        color: #000;
      }
      a {
        color: #000;
      }
    </style>
    <title><%= content_for(:title) || "Gitrack" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Gitrack">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="margin: 0; padding: 0; background-color: #ffffff; color: #000;">
    <!-- Header -->
    <header style="background-color: #f5f5f5; padding: 15px 20px; border-bottom: 1px solid #ddd;">
      <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 15px;">
        <%= link_to root_path, style: "display: flex; align-items: center; text-decoration: none; flex-shrink: 0;" do %>
          <%= image_tag "tummy.png", alt: "Gitrack Logo", style: "max-width: 50px; height: auto;" %>
        <% end %>
        <h1 style="margin: 0; font-size: 20px; font-weight: bold; color: #000; flex: 1;">
          <%= content_for(:page_title) || "Gitrack" %>
        </h1>
        <% if ENV['TUMMY_VERSION'].present? %>
          <div style="font-size: 10px; color: #666; word-break: break-all; flex-shrink: 0;">
            <%= ENV['TUMMY_VERSION'] %>
          </div>
        <% end %>
      </div>
      <div style="display: flex; gap: 10px; flex-wrap: wrap;">
        <%= link_to "Add Food", new_food_entry_path, style: "background-color: #4CAF50; color: white; padding: 10px 16px; text-decoration: none; border-radius: 4px; font-size: 14px; white-space: nowrap;" %>
        <%= link_to "Add BM", new_bowel_movement_path, style: "background-color: #9C27B0; color: white; padding: 10px 16px; text-decoration: none; border-radius: 4px; font-size: 14px; white-space: nowrap;" %>
        <%= link_to "Add Accident", new_accident_path, style: "background-color: #FF9800; color: white; padding: 10px 16px; text-decoration: none; border-radius: 4px; font-size: 14px; white-space: nowrap;" %>
        <%= link_to "All Entries", timeline_path, style: "background-color: #607D8B; color: white; padding: 10px 16px; text-decoration: none; border-radius: 4px; font-size: 14px; white-space: nowrap;" %>
        <%= link_to "List Foods", food_entries_path, style: "background-color: #f2f2f2; color: #000; padding: 10px 16px; text-decoration: none; border-radius: 4px; font-size: 14px; white-space: nowrap; border: 1px solid #ddd;" %>
        <%= link_to "List BMs", bowel_movements_path, style: "background-color: #f2f2f2; color: #000; padding: 10px 16px; text-decoration: none; border-radius: 4px; font-size: 14px; white-space: nowrap; border: 1px solid #ddd;" %>
        <%= link_to "List Accidents", accidents_path, style: "background-color: #f2f2f2; color: #000; padding: 10px 16px; text-decoration: none; border-radius: 4px; font-size: 14px; white-space: nowrap; border: 1px solid #ddd;" %>
      </div>
    </header>
    
    <!-- Main Content -->
    <main style="padding: 20px; background-color: #ffffff;">
      <%= yield %>
    </main>
    
    <script>
      // Detect browser timezone and set it as a cookie for the server to use
      (function() {
        try {
          const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone;
          // Set cookie that expires in 1 year
          const expires = new Date();
          expires.setFullYear(expires.getFullYear() + 1);
          document.cookie = 'user_timezone=' + encodeURIComponent(timezone) + 
                           '; expires=' + expires.toUTCString() + 
                           '; path=/; SameSite=Lax';
        } catch (e) {
          // Fallback: browser doesn't support timezone detection
          console.warn('Could not detect browser timezone:', e);
        }
      })();
    </script>
  </body>
</html>
